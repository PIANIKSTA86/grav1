# ============================================================================
# GRAVI SAAS - CONFIGURACIÓN DE ENTORNO
# ============================================================================
# Este archivo contiene todas las variables de entorno necesarias.
# INSTRUCCIONES:
# 1. Copia este archivo y renómbralo a .env.local
# 2. Reemplaza los valores [PLACEHOLDER] con tus datos reales
# 3. NUNCA comitees .env.local - está en .gitignore
# ============================================================================

# ============================================================================
# 🗄️  BASE DE DATOS - ELIGE UNA OPCIÓN
# ============================================================================

# OPCIÓN A: PostgreSQL Local
# Descomenta si usas PostgreSQL instalado localmente
# Asegúrate de crear la BD primero: createdb gravi
# DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/gravi

# OPCIÓN B: Neon (PostgreSQL Serverless) - RECOMENDADO
# 1. Ve a https://console.neon.tech
# 2. Crea un proyecto
# 3. Copia el connection string y pégalo aquí
DATABASE_URL=postgresql://[USERNAME]:[PASSWORD]@[HOST].neon.tech/gravi?sslmode=require

# OPCIÓN C: Supabase (PostgreSQL + extras)
# 1. Ve a https://supabase.com
# 2. Crea un proyecto
# 3. Copia el connection string
# DATABASE_URL=postgresql://[user]:[password]@[host]:5432/postgres

# ============================================================================
# 🖥️  SERVIDOR
# ============================================================================

# Puerto en el que escucha el backend
PORT=5000

# Entorno de ejecución
NODE_ENV=development

# Nivel de log (debug, info, warn, error)
LOG_LEVEL=debug

# ============================================================================
# 🔐 SEGURIDAD
# ============================================================================

# Secreto para firmar sesiones - CAMBIA ESTO A UN VALOR ÚNICO Y FUERTE
# Mínimo 32 caracteres. Puedes usar: openssl rand -base64 32
SESSION_SECRET=tu-secreto-super-largo-aqui-minimo-32-caracteres-por-favor

# JWT Secret para tokens (si se implementa JWT)
JWT_SECRET=jwt-secret-super-largo-aqui-minimo-32-caracteres

# Hash salt para bcrypt
BCRYPT_ROUNDS=10

# ============================================================================
# 🌐 URLs Y DOMINIOS
# ============================================================================

# URL del frontend (para CORS)
FRONTEND_URL=http://localhost:5173

# URL del backend (para referencias)
BACKEND_URL=http://localhost:5000

# Dominio principal
MAIN_DOMAIN=localhost

# ============================================================================
# 📧 EMAIL (Futuro)
# ============================================================================

# SMTP - Para envío de emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu-email@gmail.com
SMTP_PASSWORD=tu-contraseña-app

# Email desde el cual se envían notificaciones
MAIL_FROM=noreply@gravi.com

# ============================================================================
# 🔑 APIs EXTERNAS (Futuro)
# ============================================================================

# Si se integra Stripe para pagos
STRIPE_PUBLIC_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_...

# Si se integra Auth0 o similar
AUTH0_DOMAIN=your-domain.auth0.com
AUTH0_CLIENT_ID=your-client-id
AUTH0_CLIENT_SECRET=your-client-secret

# ============================================================================
# 🧪 TESTING
# ============================================================================

# Base de datos para tests
TEST_DATABASE_URL=postgresql://postgres:password@localhost:5432/gravi_test

# ============================================================================
# 📝 NOTAS IMPORTANTES
# ============================================================================
#
# 1. NUNCA comitees este archivo (.env.local está en .gitignore)
# 2. NUNCA subas secrets a repositorio público
# 3. Para producción, usar gestor de secrets (AWS Secrets Manager, etc)
# 4. Las variables de entorno se leen automáticamente al iniciar
# 5. TypeScript + Vite pueden validar variables con constantes tipadas
#
# ============================================================================
